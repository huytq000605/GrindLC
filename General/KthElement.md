# Finding the Kth Element

**Idea:** To find the *k*-th element (smallest/largest), reach for either **binary search** (search over the answer value) or a **priority queue** (heap). Binary search tends to handle edge cases more gracefully and offers better performance.
