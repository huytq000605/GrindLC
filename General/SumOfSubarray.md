# Sum of Subarray (Prefix Sum)

**Idea:** Whenever you need the sum of a subarray, think about **prefix sums** — once you have a prefix sum array, the sum of any subarray can be computed directly from it.
